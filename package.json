{
    "name": "noroshi",
    "version": "1.0.0",
    "description": "Noroshi Landing Pages - Unified Development Environment",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm run dev:toB\" \"npm run dev:toC\" \"npm run dev:toW\"",
        "dev:toB": "cd toB-lp && npm run dev",
        "dev:toC": "cd toC-lp && npm run dev",
        "dev:toW": "cd toW-lp && npm run dev",
        "build": "npm run build:toB && npm run build:toC && npm run build:toW",
        "build:toB": "cd toB-lp && npm run build",
        "build:toC": "cd toC-lp && npm run build",
        "build:toW": "cd toW-lp && npm run build",
        "install:all": "npm install && cd toB-lp && npm install && cd ../toC-lp && npm install && cd ../toW-lp && npm install"
    },
    "devDependencies": {
        "concurrently": "^9.1.0"
    }
}